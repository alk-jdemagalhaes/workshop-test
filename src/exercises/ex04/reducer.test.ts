import { reducer } from "./reducer";
import { increaseCounter, setcounter } from "./actions";

describe("Ex04Reducer", () => {
  beforeEach(() => {
    //expect.hasAssertions();
  });

  it("should increase the counter", () => {
    // remember what you need : your state, an action, the reducer
    // Combine the initial state and the reducer. Now mix it with the action. Boom, your final state !
    // Now just make sure your final state has the expected result.
  });

  it("should set the counter", () => {});
});
